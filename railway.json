{
  "deploy": {
    "startCommand": "python manage.py shell -c \"from django.contrib.auth.models import User; User.objects.filter(username='Elvis').delete(); User.objects.create_superuser('Elvis', 'elvis@admin.com', '5472')\" && gunicorn gif.wsgi:application",
    "restartPolicyType": "ON_FAILURE"
  }
}
